/**
 * config-notes-master.js
 * Context bridge & external initialization hook for Configuration & Notes Master.
 * Primary UI logic is contained in config-notes-master.html.
 */

window.init_config_notes_master = async function () {
  if (typeof window.cnmSaveSettings === 'function') {
    // Component loaded and initialized in HTML
  }
};
